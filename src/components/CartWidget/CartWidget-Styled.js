import styled from "styled-components";

export const CartIcon = styled.div `
    display: flex;
    align-items: center;
    margin: 0;
    padding: 0;

    svg {
        color: #EEE;
        font-size: 2.5rem;
    }

    span {
        color: white;
        background-color: red;
        padding: 2px;
    }
`